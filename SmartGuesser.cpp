#include<iostream>
#include "SmartGuesser.hpp"
#include<string>
using namespace std;

string SmartGuesser::guess() 
{
	
	return "0000";	
	/*if (this->s == "0000") 
	{
		int i;
		string Temperory_str;
		for(i=0;i<this->length;i++)
		{
			Temperory_str.at(i) = s.at(i);
		}
		s = Temperory_str;

	}
	
	
		if ((this->Memory_Pgia == this->lastpgia) && this->length>=s.length()) {
			s.at(this->index) = s.at(this->index) + 1;

			return s;
		}
	    

		if ((this->Memory_Pgia + 1 == this->lastpgia) && (this->length >= s.length())) {
			this->Memory_Pgia++;
			this->index = this->index + 1;
			s.at(this->index) = s.at(this->index) + 1;

			return s;
		}*/
		
}
